let inputWidth;
let inputHeight;

inputWidth = parseInt(prompt("Enter the width")) ;
inputHeight = parseInt(prompt("Enter the Height")) ;

let area = inputWidth*inputHeight;

alert(`the area is=55 ${area}`)